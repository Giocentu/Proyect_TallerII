<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="MiProyecto.DashboardWindow"
        Title="Restaurante - Menú Principal"
        Width="900" Height="600"
        WindowStartupLocation="CenterScreen"
        Background="#ECF0F1">

    <Grid RowDefinitions="Auto, *">
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <StackPanel Orientation="Horizontal" Spacing="15">
                <TextBlock Text="👤" FontSize="24" VerticalAlignment="Center"/>
                
                <StackPanel>
                    <TextBlock Name="txtBienvenida" 
                               Text="Bienvenido" 
                               Foreground="White" 
                               FontSize="18" 
                               FontWeight="Bold"/>
                    <TextBlock Name="txtRol" 
                               Text="Rol: ..." 
                               Foreground="#BDC3C7" 
                               FontSize="14"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <WrapPanel Grid.Row="1" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   Margin="20">
            
            <Button Name="btnPersonal" Width="180" Height="120" Margin="15" Background="#E74C3C" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="👥" FontSize="35" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Personal" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Name="btnCaja" Width="180" Height="120" Margin="15" Background="#27AE60" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="💰" FontSize="35" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Caja / Finanzas" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Name="btnReservas" Width="180" Height="120" Margin="15" Background="#3498DB" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="📅" FontSize="35" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Reservas" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Name="btnMesas" Width="180" Height="120" Margin="15" Background="#F39C12" CornerRadius="8">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🍽️" FontSize="35" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Estado Mesas" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

        </WrapPanel>

        <Button Content="Cerrar Sesión" 
                Grid.Row="1" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom" 
                Margin="20"
                Background="#95A5A6"
                Click="BtnSalir_Click"/>
    </Grid>
</Window>